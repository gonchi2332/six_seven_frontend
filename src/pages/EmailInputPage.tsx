import EmailInputPopup from "../features/auth/components/EmailPasswordVerification/EmailInputPopup";

const EmailInputPage = () => {
  return (
    <div>
      <EmailInputPopup />
    </div>
  );
};

export default EmailInputPage;